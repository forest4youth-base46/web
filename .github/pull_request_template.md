## What changed and why

<!-- The "why" matters more than the "what" here — the diff already shows what changed. -->

## Verification

- [ ] `npm run check:i18n` passes
- [ ] `npm run test:unit` passes
- [ ] `npm test` (full smoke suite) passes
- [ ] If this touched the mobile Pocketbook drawer, Run Mode, or cascade-order-sensitive CSS: checked visually in a real browser, not just the test suite
- [ ] If this split or moved a file: reassembly diffed byte-for-byte identical to the original before deleting it

## Anything deliberately left out or deferred

<!-- e.g. "found X while working on this, didn't fix it because Y" — worth a line even if it's "nothing." -->
